# Resume Monorepo

This repository keeps the public GitHub Pages site and the resume source assets together.

- `index.html` remains at the repository root so the public site still loads from the domain root.
- `resume/` contains the imported files from the former `MyResume` repository.

GitHub Pages should continue to serve `index.html` as the homepage when this repository is published from the root of the `main` branch.
